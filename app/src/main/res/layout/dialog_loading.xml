<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:minWidth="@dimen/dp_60"
        android:minHeight="@dimen/dp_60"
        android:background="@drawable/shape_solid_ffffffff_radius_4dp"
        android:orientation="vertical"
        android:padding="@dimen/dp_16">

        <cc.bear3.osbear.ui.common.AnimatableImageView
            android:id="@+id/image"
            android:layout_width="@dimen/dp_36"
            android:layout_height="@dimen/dp_36"
            android:src="@drawable/anim_vector_loading" />

        <TextView
            android:id="@+id/hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dp_8"
            android:maxWidth="@dimen/dp_160"
            android:textColor="@color/app_text_summary"
            android:textSize="14sp"
            tools:text="Loading" />

    </LinearLayout>

</layout>